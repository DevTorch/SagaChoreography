server:
  port: 8082

spring:
  application:
    name: stock-service
  config:
    import: optional:file:.env[.properties]
  docker:
    compose:
      file: docker-compose.yaml
      lifecycle-management: start_only